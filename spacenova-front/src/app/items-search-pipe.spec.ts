import { ItemsSearchPipe } from './items-search-pipe';

describe('ItemsSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemsSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
